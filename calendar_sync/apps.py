"""App config for the calendar_sync app."""
from django.apps import AppConfig


class CalendarSyncConfig(AppConfig):
    """App config for the calendar_sync app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'calendar_sync'
